# gravatar.bluecdn.com — 源站 Caddy 配置(参考)
#
# 从芬兰源站 65.109.62.100 的 /etc/caddy/Caddyfile 导出留档。
# 完整文件还含 bluecdn.com 主站、static、mapcdn、yite.net 等站点,这里只截取 gravatar 部分。
#
# 要点:
#   - 全局 auto_https off,证书由 acme.sh 用 dns_aliesa 插件签发后手工加载(tls_wild 片段)
#   - 后端 LiteAvatar 监听 127.0.0.1:8787,只走本机回环
#   - CORS 全开,供 WordPress / 论坛 / 博客跨域引用头像
#
# 历史:这里曾放 caddy-sh.Caddyfile / caddy-us.Caddyfile(上海 + 硅谷双源站),
# 那两台已下线或改作他用,文件于 2026-08-02 删除。

gravatar.bluecdn.com {
	import tls_wild
	encode gzip zstd
	header Access-Control-Allow-Origin "*"
	reverse_proxy 127.0.0.1:8787
}

http://gravatar.bluecdn.com {
	encode gzip zstd
	header Access-Control-Allow-Origin "*"
	reverse_proxy 127.0.0.1:8787
}
