# /etc/systemd/system/bunny-stats.timer
# 每小时拉一次 Bunny CDN 累计请求数（启动后 3 分钟先跑一次，之后每小时整点附近）。
[Unit]
Description=Hourly Bunny CDN stats fetch

[Timer]
OnBootSec=3min
OnUnitActiveSec=1h
AccuracySec=30s
Persistent=true

[Install]
WantedBy=timers.target
